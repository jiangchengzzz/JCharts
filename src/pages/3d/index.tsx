import React, { PureComponent } from 'react';
import Redirect from 'umi/redirect'

class ThreeDimension extends PureComponent {
  render() {
    return <Redirect to={'3d/pie'} />
  }
}

export default ThreeDimension
